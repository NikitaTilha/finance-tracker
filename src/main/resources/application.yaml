spring:
  application:
    name: finance-tracker

  datasource:
    url: jdbc:postgresql://127.0.0.1:15432/finance_tracker
    username: finance_user
    password: finance_pass

  flyway:
    enabled: true

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
    show-sql: true